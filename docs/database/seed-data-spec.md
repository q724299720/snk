# 冷启动种子数据规范

## 文档职责

本文件定义 `Phase 0` 冷启动种子数据的字段要求、最低规模、来源优先级和导入前校验规则。

## 种子数据目标

首批数据需要同时支持：

- 文本搜索
- 条形码检索
- OCR 文本召回
- 基础记录创建

## 首批数据来源优先级

1. 自建整理的高频品牌与高频菜品
2. 外部开放食品数据库
3. 人工补充高频 OCR 关键词
4. 后续用户创建的 `pending` 条目

## 建议最小字段集

种子数据至少包含以下字段：

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `name` | 是 | 主展示名 |
| `item_type` | 是 | `packaged_product / dish / fruit`，与 `FoodItem` 建模保持一致 |
| `category` | 是 | 一级分类 |
| `subcategory` | 否 | 二级分类 |
| `brand` | 否 | 品牌 |
| `barcode` | 否 | 包装食品优先补齐 |
| `alias` | 否 | 常见简称 |
| `search_keywords` | 是 | 搜索和 OCR 召回关键词 |
| `tags` | 否 | 口味 / 场景 / 功能标签 |
| `source` | 是 | `system / external_api` |
| `audit_status` | 是 | 首批系统数据统一为 `approved` |

## 手动创建 pending 条目最小字段

当搜索、扫码、OCR、图像识别都失败时，用户手动创建 `pending FoodItem` 的最小必填字段为：

- `name`
- `item_type`
- `category`

其他字段如 `brand`、`barcode`、`alias`、`search_keywords`、`tags`、图片均为选填，后续可由审核补全。

其中分类字段规则为：

- 一级分类 `category` 必选
- 二级分类 `subcategory` 选填

图片字段规则为：

- 图片选填，不作为手动创建 `pending FoodItem` 的必需条件
- 若用户提供图片，可用于后续审核、OCR 补全和条目纠错

品牌字段规则为：

- `brand` 选填
- MVP 手动创建表单中直接提供可选 `brand` 输入框
- 对包装食品条目，用户若能补充品牌，将显著提升审核、搜索和去重质量

关键词字段规则为：

- `search_keywords` 不暴露给普通用户手动填写
- 系统根据 `name + brand + category + subcategory` 自动生成基础关键词
- 后台审核时允许人工修正或补充关键词

## search_keywords 规则

`search_keywords` 建议包含：

- 品牌名
- 品类名
- 口味词
- OCR 易识别出的包装文字
- 常见错别字或简称

示例：

```text
乐事,薯片,黄瓜味,青柠味,袋装,休闲零食
```

## 已知有效词来源

用于自动拒绝规则中的“有效词”判断，MVP 阶段采用双来源：

1. 种子数据词表
2. 小型手工词典

手工词典优先覆盖高频短词和高误伤词，例如：

- 茶
- 梨
- 面
- 饭
- 粥
- 粉

维护方式约束：

- 小型手工词典不随 App 发版维护
- MVP 阶段由服务端数据库配置表承载
- 支持后台热更新，审核规则读取最新生效词典
- 后台保存后立刻生效，后续审核任务不等待额外缓存刷新
- 自动拒绝阈值不放入该配置表，继续由服务端代码配置维护

## 首批数据最低规模

- 总条目数：3000 条以上
- 带条形码条目：1500 条以上
- 每个一级分类至少 50 条
- 每个高优先品牌至少 20 条

## 导入前校验规则

### 必过校验

- `name` 不为空
- `category` 必须属于已定义一级分类
- `source` 必须合法
- `audit_status` 必须合法

### 条形码校验

- 同一条形码不能映射多个不同 `FoodItem`
- 条形码为空的条目必须有足够的 `search_keywords`

### 搜索召回校验

- `name + alias + search_keywords` 拼接后长度不能过短
- 高优先品牌条目必须至少含有品牌词和口味词 / 品类词之一

## 建议文件格式

首批导入建议使用 CSV 或 TSV，方便人工整理和脚本处理。

建议列顺序：

```text
name,item_type,category,subcategory,brand,barcode,alias,search_keywords,tags,source,audit_status
```

## 当前仓库启动样本

当前仓库提供一份 `Phase 0 / Step 3` 用的起步样本：

- `data/seed/food-items-sample.csv`

使用约束：

- 该文件用于字段对齐、导入脚本开发和最小搜索验证
- 文件中的条形码样本默认按演示数据处理，正式导入生产库前应替换为真实可校验数据
- 后续批量扩充时，仍需满足本文件中的字段规范与导入校验规则

## Phase 0 Step 1 验收标准

- 已定义最小字段集
- 已定义最低规模
- 已定义导入前校验规则
- 可直接指导后续编写导入脚本或整理 CSV

## 变更记录维护规则

- 每次修改本文件时，必须在下方追加一条记录

## 变更记录

| 日期 | 修改人 | 变更范围 | 原因 |
| --- | --- | --- | --- |
| 2026-06-13 | Codex | 新建冷启动种子数据字段规范与校验规则文档 | 作为 Phase 0 第一步，为后续整理首批数据和写导入脚本提供标准 |
| 2026-06-13 | Codex | 明确手动创建 pending 条目的最小字段 | 已确认兜底创建流程只强制 `name + item_type + category` |
| 2026-06-13 | Codex | 明确手动创建时分类字段的填写规则 | 已确认一级分类必选、二级分类选填 |
| 2026-06-13 | Codex | 明确手动创建时图片为选填 | 已确认失败兜底链路不应强制要求图片 |
| 2026-06-13 | Codex | 明确手动创建时 `brand` 为可选直出字段 | 已确认品牌对包装食品审核和去重有高价值，应直接暴露在表单中 |
| 2026-06-13 | Codex | 明确 `search_keywords` 由系统自动生成 | 已确认关键词是系统字段，不应增加普通用户输入负担 |
| 2026-06-13 | Codex | 明确已知有效词来源 | 已确认自动拒绝需要依赖种子词表与小型手工词典共同降低误伤 |
| 2026-06-13 | Codex | 明确小型手工词典由数据库配置表热更新维护 | 已确认该词典需要在线调整，不应依赖客户端或服务端发版 |
| 2026-06-13 | Codex | 明确热更新词典保存后立刻生效 | 已确认不需要增加词典缓存延迟刷新机制 |
| 2026-06-13 | Codex | 明确自动拒绝阈值不进入热更新词典配置 | 已确认 MVP 阶段只允许在线维护词典，不在线调整阈值 |
| 2026-06-13 | Codex | 为种子数据补充 `item_type` 字段并登记仓库启动样本路径 | 对齐既有 `FoodItem` 建模，支撑 Phase 0 样本整理与后续导入脚本开发 |
