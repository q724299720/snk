# 产品需求说明

## 文档职责

本文件记录产品定位、目标用户、MVP 范围、扩展范围与冷启动策略。

## 产品目标

打造一个安卓端 App，面向“美食 / 零食发现、识别、记录、分享”场景，核心能力如下：

- 用户可通过输入名称搜索美食或零食
- 用户可通过扫码、OCR 或图片识别定位食物
- 用户可对食物进行评分、备注、打标签
- 所有业务数据保存在远程服务器
- 手机端负责采集、搜索、提交与展示
- 服务端负责数据存储、识别编排、审核与运营控制

## 目标用户

- 喜欢记录日常饮食的人
- 喜欢尝试零食并做评分的人
- 想通过图片快速识别食物的人
- 想记录“吃过什么、喜不喜欢、是否复购”的人

## MVP 范围

### 客户端 MVP

- 游客模式 / 匿名本地用户
- 首页最近记录 / 推荐入口
- 最近搜索（本地缓存）
- 文本搜索食物
- 条形码扫码录入零食 / 包装食品
- 拍照或上传图片识别食物
- 本地 OCR 提取品牌和品名
- 查看识别结果和候选项
- 创建记录、评分、备注、标签
- 离线草稿箱与弱网补传
- 查看个人历史记录

### 服务端 MVP

- 用户体系
- 食物基础资料库
- 冷启动数据导入与外部食品数据兜底
- 图片上传与存储
- 条形码检索
- OCR 兜底识别
- 图像识别任务接口
- 搜索接口
- 记录与评分存储
- 后台管理：食物条目管理、用户记录审核、标签管理

## 第二阶段扩展范围

- 社区分享与点赞评论
- 收藏清单 / 想吃清单
- 地点关联
- 营养信息展示
- 增强版 OCR 结构化提取
- 识别结果纠错与模型反馈闭环
- 多模态检索

## 冷启动与数据增长策略

这是最优先解决的问题。

- 首批导入常见零食、饮料、菜品基础数据，至少准备数千条可搜索数据
- 早期接入外部开放食品数据库作为兜底来源
- 搜索或识别失败时，允许用户手动创建记录
- 用户手动创建的食物条目以 `pending` 状态进入候选池
- 手动创建 `pending` 条目时，最小必填字段仅为 `name + item_type + category`
- 手动创建时一级分类必选，二级分类选填
- 手动创建时图片选填，不作为兜底创建前置条件
- 手动创建表单直接提供可选 `brand` 输入框
- 手动创建表单不暴露 `search_keywords`，由系统自动生成基础关键词
- 手动创建 `pending FoodItem` 成功后，自动回填当前记录并继续保存本次 `FoodRecord`
- 创建者在搜索结果里看到自己的 `pending FoodItem` 时，应有明确“待审核”标记
- 对创建者本人而言，搜索结果中 `approved` 条目默认排在前，`pending` 条目靠后展示
- 用户一旦进入手动创建流程，后续即使发现匹配候选，也只做可选提示，不自动打断当前输入
- 若 `FoodRecord` 绑定的是 `pending FoodItem`，则在最近记录中也应显示轻量“待审核”提示
- 待后台审核后，条目才可全局可见
- 用 UGC 持续补足长尾食物和新品零食

## 产品边界原则

- 第一阶段不做复杂社交功能
- 第一阶段不自研识别模型
- 第一阶段优先做可闭环的数据流和记录体验
- 第一阶段优先游客模式，不先做完整登录体系
- 游客模式在技术上使用服务端匿名用户，不采用纯本地匿名模型
- 游客匿名身份在 MVP 阶段采用安装级生命周期，卸载重装后视为新游客
- 零食识别场景优先使用条形码和 OCR，不把纯视觉识别作为主路径
- MVP 阶段不引入独立的分享 / 帖子对象
- 若未来扩社区，分享对象应独立于 `FoodRecord` 设计
- MVP 首页不做个性化推荐算法
- 首页“推荐”仅指最近记录、快捷入口、常见分类和少量热门静态内容
- 首页中的“热门静态推荐”在 MVP 阶段采用手工配置来源
- 首页最近记录默认展示最近 `5` 条
- 首页正式最近记录与待上传草稿 / 失败重试项分区展示

## 变更记录维护规则

- 每次修改本文件时，必须在下方追加一条记录

## 变更记录

| 日期 | 修改人 | 变更范围 | 原因 |
| --- | --- | --- | --- |
| 2026-06-13 | Codex | 从 `agents.md` 拆出产品范围、用户、MVP 与冷启动策略 | 将项目总文档重构为索引式看板 |
| 2026-06-13 | Codex | 明确 MVP 阶段不引入独立分享对象 | 已确认记录闭环优先，社区分享延后到后续阶段再独立设计 |
| 2026-06-13 | Codex | 收敛首页推荐语义 | 已确认 MVP 首页不做算法推荐，只保留记录延续和快捷入口能力 |
| 2026-06-13 | Codex | 明确热门静态推荐采用手工配置来源 | 已确认首页少量推荐内容不从实时行为动态计算 |
| 2026-06-13 | Codex | 明确首页最近记录默认展示 5 条 | 已确认首页信息密度和可用性需要保持平衡 |
| 2026-06-13 | Codex | 明确首页记录与草稿分区展示 | 已确认正式历史与未完成草稿不能混排展示 |
| 2026-06-13 | Codex | 明确最近搜索进入 MVP 且只做本地缓存 | 已确认搜索体验需要基础延续，但无需引入服务端同步复杂度 |
| 2026-06-13 | Codex | 明确游客模式先行 | 已确认 MVP 第一阶段优先打通匿名闭环，而不是先做完整登录体系 |
| 2026-06-13 | Codex | 明确游客模式采用服务端匿名用户 | 已确认匿名体验下仍需稳定的远程用户标识，以支撑同步与持久化 |
| 2026-06-13 | Codex | 明确匿名身份采用安装级生命周期 | 已确认卸载重装后匿名身份不恢复，跨设备恢复留给正式登录体系 |
| 2026-06-13 | Codex | 明确手动创建 pending 条目的最小必填字段 | 已确认失败兜底创建不应要求过多输入 |
| 2026-06-13 | Codex | 明确手动创建时一级分类必选、二级分类选填 | 已确认失败兜底表单需要兼顾速度和基本结构化信息 |
| 2026-06-13 | Codex | 明确手动创建时图片为选填 | 已确认失败兜底体验优先，不强制用户提供图片 |
| 2026-06-13 | Codex | 明确手动创建表单直接提供可选 `brand` 字段 | 已确认品牌信息对包装食品场景有较高价值 |
| 2026-06-13 | Codex | 明确手动创建表单不暴露 `search_keywords` | 已确认关键词由系统自动生成更符合 MVP 体验 |
| 2026-06-13 | Codex | 明确手动创建成功后自动继续当前记录流程 | 已确认失败兜底链路不能中断用户当前记录行为 |
| 2026-06-13 | Codex | 明确创建者看到 pending 条目时需显示待审核标记 | 已确认用户需要感知条目尚未正式纳入系统 |
| 2026-06-13 | Codex | 明确搜索结果中正式条目优先于 pending 条目 | 已确认 pending 条目只是兜底，不应抢占正式条目的默认位置 |
| 2026-06-13 | Codex | 明确手动创建流程不被后续匹配结果自动打断 | 已确认兜底创建一旦开始，应以用户当前输入为主 |
| 2026-06-13 | Codex | 明确绑定 pending 条目的记录也需显示待审核提示 | 已确认用户在记录列表中也需要感知条目状态未正式稳定 |
