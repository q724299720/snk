server:
  forward-headers-strategy: framework
  tomcat:
    threads:
      max: ${SNK_TOMCAT_MAX_THREADS:24}
      min-spare: ${SNK_TOMCAT_MIN_SPARE_THREADS:2}
    max-connections: ${SNK_TOMCAT_MAX_CONNECTIONS:64}
    accept-count: ${SNK_TOMCAT_ACCEPT_COUNT:25}
    connection-timeout: ${SNK_TOMCAT_CONNECTION_TIMEOUT:20s}

spring:
  application:
    name: snk-server
  config:
    import:
      - optional:file:${SNK_RUNTIME_CONFIG:./runtime/application-runtime.yml}
      - optional:file:./server/runtime/application-runtime.yml
      - optional:file:../runtime/application-runtime.yml
      - optional:file:../../runtime/application-runtime.yml
  servlet:
    multipart:
      max-file-size: ${SNK_UPLOAD_MAX_FILE_SIZE:10MB}
      max-request-size: ${SNK_UPLOAD_MAX_REQUEST_SIZE:10MB}
      file-size-threshold: 0B
  datasource:
    url: jdbc:postgresql://${SNK_DB_HOST:localhost}:${SNK_DB_PORT:5432}/${SNK_DB_NAME:snk}
    username: ${SNK_DB_USERNAME:snk}
    password: ${SNK_DB_PASSWORD:snk}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: ${SNK_DB_MAX_POOL_SIZE:3}
      minimum-idle: ${SNK_DB_MIN_IDLE:0}
      idle-timeout: ${SNK_DB_IDLE_TIMEOUT_MS:120000}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  sql:
    init:
      mode: never

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true

logging:
  level:
    org.flywaydb: info

snk:
  admin:
    api-token: ${SNK_ADMIN_API_TOKEN:}
    token-header: ${SNK_ADMIN_TOKEN_HEADER:X-SNK-ADMIN-TOKEN}
  storage:
    root-path: ${SNK_STORAGE_ROOT:./runtime/storage}
    public-path: ${SNK_STORAGE_PUBLIC_PATH:/uploads}
    public-base-url: ${SNK_STORAGE_PUBLIC_BASE_URL:https://snk.qiuxinmin.cn}
    image-directory: ${SNK_STORAGE_IMAGE_DIRECTORY:images}
    max-image-size-bytes: ${SNK_STORAGE_MAX_IMAGE_SIZE_BYTES:10485760}
  recognition:
    ocr:
      provider: ${SNK_RECOGNITION_OCR_PROVIDER:disabled}
      stub-text: ${SNK_RECOGNITION_OCR_STUB_TEXT:}
    image:
      provider: ${SNK_RECOGNITION_IMAGE_PROVIDER:stub}
      stub-query: ${SNK_RECOGNITION_IMAGE_STUB_QUERY:乐事黄瓜味}
  moderation:
    auto-audit:
      enabled: ${SNK_MODERATION_AUTO_AUDIT_ENABLED:true}
      pending-age-hours: ${SNK_MODERATION_PENDING_AGE_HOURS:24}
      fixed-delay-ms: ${SNK_MODERATION_AUTO_AUDIT_FIXED_DELAY_MS:3600000}
      initial-delay-ms: ${SNK_MODERATION_AUTO_AUDIT_INITIAL_DELAY_MS:300000}
      reject-keywords:
        - test
        - junk
        - garbage
        - unknown
        - asdf
        - qwer
